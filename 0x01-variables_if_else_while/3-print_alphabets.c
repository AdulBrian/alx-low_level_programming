#include <stdio.h>

/**
 *  * main - print alphabets in small lettere,
 *    * a new line follows
 *     * Return: Always 0 (if Successful)
 *      */

int main(void)
{
		char ch;

			for (ch = 'a'; ch <= 'z'; ch++)
					{
								putchar(ch);
									}

